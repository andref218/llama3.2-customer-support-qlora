[project]
name = "llm-customer-support-finetuning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.14.0",
    "bitsandbytes==0.48.2; platform_system == 'Linux'",
    "datasets>=5.0.0",
    "huggingface_hub",
    "peft",
    "python-dotenv>=1.2.2",
    "torch>=2.13.0",
    "transformers>=5.14.1",
    "trl==0.25.1",
    "wandb>=0.28.1",
]
